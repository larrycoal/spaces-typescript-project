import React from 'react';


const Header = () => {
    return (
        <nav className="header">
            <span>Spaces</span>
            <button>LogOut</button>
        </nav>
    );
};

export default Header;